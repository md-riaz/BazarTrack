enum ItemStatus { pending, done, notFound, cancelled }

enum BazarStatus { draft, open, closed, cancelled }

enum EntryType { moneyReceived, moneyReturned, adjustment }

enum UserRole { assistant, owner, admin }

enum SyncStatus { online, syncing, offline, failed }

enum WalletType { shared, personal, event, other }

enum AttachmentType { image, pdf }
